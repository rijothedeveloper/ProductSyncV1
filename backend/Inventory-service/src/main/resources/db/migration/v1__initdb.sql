create table inventory (
                           id bigint not null,
                           quantity bigint,
                           sku varchar(255),
                           primary key (id)
)